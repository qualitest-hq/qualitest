# 可测提示词 · 全量

本文件供管理端「复制可测提示词（全量）」接口读取。
文件开头到下方分隔线为说明；接口只返回分隔线之后的正文。

用法：复制后贴到 Cursor 等 AI 编辑器（打开业务仓库），让其按模块汇总可测短提示；再分批贴回质衡测试流 AI 造流。

---

请阅读本仓库业务模块，按模块列出可直接贴进质衡测试流 AI 的短提示。
每个模块先给主路径，重要写操作再补失败/拒绝类提示（以代码里真实分支为准）。
每条用业务语言写步骤、测值和期望结果；不要写 URL 与接口编排细节。
测值从仓库文档与种子数据读取；读不到标「不确定」。
按模块分段输出，便于我分批粘贴。
登录一般由质衡项目里已挂的登录流覆盖，提示里不用重复写，除非本条就是在测登录。
