# 可测提示词 · 增量

本文件供管理端「复制可测提示词（增量）」接口读取。
文件开头到下方分隔线为说明；接口只返回分隔线之后的正文。

用法：复制后贴到 Cursor 等 AI 编辑器（打开业务仓库），让其按本次改动写出短提示；再把产出贴回质衡测试流 AI 造流。

---

请阅读我指定的改动（diff 或目录），为质衡测试流 AI 写出若干条短提示。
每条只测一个意图；用业务语言写步骤、测值和期望结果。
不要写 URL、HTTP 方法、JSONPath；不要写成长篇方案。
测值从 README、配置、种子数据里取；读不到就标明「不确定」，不要编造 ID。
只覆盖本次改动相关能力。
登录一般由质衡项目里已挂的登录流覆盖，提示里不用重复写，除非本条就是在测登录。
