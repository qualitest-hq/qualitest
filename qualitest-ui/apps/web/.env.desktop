# 页面标题
VITE_APP_TITLE = 质衡

# Electron 桌面安装包构建（loadFile + file://）
VITE_APP_ENV = 'desktop'

# 质衡后端 API 前缀（主进程 IPC 拼接路径时使用）
VITE_APP_BASE_API = '/prod-api'

# file:// 下使用 hash 路由，避免 history 模式不可用
VITE_APP_ROUTER_MODE = hash

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
