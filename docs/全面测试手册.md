# 全面测试手册（含 Cursor 自动测试）

> **用途**：质衡主链路 + Demo 业务的系统化验收（T1 门禁 → T2 全量 → T3 进阶）。  
> **工作区**：`qualitest-all`（`qualitest`（含 `qualitest-ui/`）· `qualitest-demo` · `qualitest-intellij-plugin`）  
> **默认账号**：质衡 / demo 管理端 `admin` / `admin123`；客户端密码一律 `Test@123456`（手机号见提示集）  
> **业务提示**：以 [AI 测试流提示集](../../qualitest-demo/docs/ai-test-flow-prompts.md) 为准；场景清单见 `qualitest-demo/sql/seed/scenarios/manifest.json`

---

## 0. 怎么跑


| 档      | 范围                                          | 是否必须       |
| ------ | ------------------------------------------- | ---------- |
| **T1** | 登录 → 环境 → 调试 → **测参文件上传** → S01 → 一条购物流 Run | **必须**（门禁） |
| **T2** | Demo 全业务（S01–S08 / F01–F12），**优先画布测试流**     | 尽力         |
| **T3** | 媒体 / **数据库快照还原** / AI / MCP / Compose / 单测  | 可选         |


**原则**

1. **门禁 = T1**；T2 尽量跑完；修不动记 §F。
2. 冒烟默认 Cursor 浏览器 MCP 代点；你起服务即可。产品 Staging ✓ / 保存 / Run 由 Agent 点，**不等** Cursor 审批卡。
3. **测的是产品路径，不是 Agent 作弊路径**：凡人手在 UI 上做不到的操作（改 Pinia / 直接改库图 JSON / 绕过 Staging 灌边等），**一律禁止当作验收手段**；卡在产品缺陷就修产品，再用人可复现方式重测。详见 **§B.0.1**。

**触发语（对 Agent）**


| 你说的话                   | 跑哪一档                |
| ---------------------- | ------------------- |
| `按全面测试手册跑最小冒烟`         | **T1**（约 30～60min）  |
| `按全面测试手册跑 Demo 全量`     | **T1 + T2**（约 2～4h） |
| `按全面测试手册全开`            | **T1 + T2 + T3**    |
| `从 T2.x 继续` / `跳过已通过项` | 按 §B.5 / §F 断点续跑    |
| `按手册 §B.0.1 铁律继续测`     | 强制人可复现口径；忽略旁路「刷绿」   |


---



## A. 环境拉起（冒烟前）



### A.1 依赖与端口

- JDK ≥ 17、Maven ≥ 3、MySQL ≥ 8、Redis ≥ 3、Node + Yarn


| 服务                 | 端口                                      | 作用                       |
| ------------------ | --------------------------------------- | ------------------------ |
| qualitest API      | **8080**                                | 主平台                      |
| qualitest-ui       | **5173**                                | 质衡 Web（`yarn dev`）       |
| qualitest-demo API | **8081**                                | 靶场 / Swagger / `baseUrl` |
| demo-ui（推荐）        | **5174**（`yarn dev`）/ **8082**（Compose） | 测试场景控制台                  |
| MySQL / Redis      | 3306 / 6379（或 demo Compose 3307 / 6380） | 依赖                       |
| RustFS（T3 媒体）      | 9000 / 9001                             | 可选；**T1 测参文件不依赖**        |




### A.2 质衡主平台

```text
1. 建库 qualitest
2. 导入 sql/qualitest_*.sql（或 Flyway 空库自迁移）
3. 确认 qualitest-admin/.../application-dev.yml 库账号 / Redis 可用
4. 根目录：mvn clean package -DskipTests  或  qualitest.bat / qualitest.sh
5. 前端：cd qualitest-ui && yarn install && yarn dev
6. 浏览器：http://localhost:5173  →  admin / admin123
```



### A.3 靶场 demo

```text
1. 建库 qualitest-demo，导入 sql/qualitest-demo_*.sql
2. 改 demo-admin/.../application-dev.yml
3. mvn clean install && demo.bat（或 demo.sh）
4. Swagger：http://localhost:8081/swagger-ui.html
5. （推荐）demo-ui：本地 `yarn dev` → http://localhost:5174；Compose → http://localhost:8082 → 测试场景控制台
6. （T3 媒体）RustFS：qualitest-demo 下 scripts\quick-start.bat rustfs
```



### A.4 自动测前自检（你做）

```text
1. 确认 5173 / 8080 / 8081（及 demo-ui：本地 5174 或 Compose 8082）已监听
2. IDEA 插件或既有数据：目标测试项目已有 demo 接口资产
3. 关验证码：`UPDATE sys_config SET config_value='false' WHERE config_key='sys.account.captchaEnabled';`（开着则登录页会停）
4. 对话触发语见 §0
```

> 缺接口资产时 Agent 只能报到「缺接口」并停在 T1。

---



## B. Cursor 自动测试（Agent 逐步清单）

> 由 **Agent + IDE Browser MCP**（及可选 qualitest MCP）代点、代查。打勾：Agent 跑通或你亲自点过。失败记 §F。  
> 业务提示正文以 [ai-test-flow-prompts.md](../../qualitest-demo/docs/ai-test-flow-prompts.md) 为准；下表只列场景 ID + 意图 + 期望，避免双份漂移。



### B.0 分工与硬规则


| 谁             | 负责                                                        |
| ------------- | --------------------------------------------------------- |
| **你**         | 起服务；插件上传接口；可选 RustFS / LLM Key |
| **Agent**     | 浏览器代点（含产品 Staging ✓ / 保存 / Run）、截图、调 MCP、按 T1→T2→T3 验收并填结果；**发现产品 bug 则修代码后人可复现重测**。**不等** Cursor 审批卡 |
| **半自动 / 可跳过** | LLM Key、qualitest MCP Token、RustFS、Compose 一键栈            |


**Agent 必须遵守**

1. 先确认端口再开浏览器；连不上就停，不空转。
2. 每小步：`browser_take_screenshot`（或等价）+ 标明 **通过 / 失败 / 跳过**。
3. iframe 点不到 → 记阻塞，改走 Swagger / curl / 调试台（仅作该步诊断；**T2 画布项仍须画布过**）。
4. 开启「允许还原被测数据」后：**同一环境串行跑**。
5. 写库场景前先加载对应场景（demo-ui 或 `POST /web/test/scenario/load/{id}`）。
6. 同一操作连续失败 ≥4 次 → 停手汇报。
7. 改画布走 Web **AI 助手 → Staging 确认 → 保存**；qualitest MCP **只读**。登录口令等优先素材库（Web AI `upsert_asset_variables` 提案 → 聊天侧确认后落盘 + `{{asset.*}}`），勿反复 flowSeed 占位。
8. **严格遵守 §B.0.1**（人可复现）；违反则该条勾选作废。
9. Staging 删除类：点 **✓ 确认删除** 即落盘删除（无二次弹窗）；自动化勿对 Staging 边按键盘 Delete。图乱则 Staging 全部 ✕ 或新建流重来。
10. **禁止空白画布当验收**：顶栏黄条「nodes 为空…」= 库里仍是新建空图（`nodes: []`）。AI Staging **未全部 ✓ 并保存**前，画布本来就是空的；**不得**把空图画布、空图保存、空图 Run 当作 T1/T2 通过。须 Staging 清零 → **保存后画布有节点** → 再 Run。
11. **画布展示不全**：先关挡着的 AI/属性侧栏，再 **拖拽画布**、点 **小地图** 聚焦，或点 **适应视图** / 缩放，把目标节点·边·Staging ✓ 挪进可视区后再点；禁止 `force` 点视口外或被挡住的控件。
12. **登录与 token 复用**：同一测试流内开头只登录一次（或挂登录子流），后续用 `{{flow.token}}` / `{{flow.adminToken}}`；勿逐步再登录。多流默认各自登录。客户端多为 `$.data.token→token`，管理端 `/login` 多为 `$.token→adminToken`。调试可用 flowSeed 预置 token，禁止 flowSeed 塞口令。
13. **不等 Cursor 审批卡**：产品 Staging ✓ 由 Agent 点。禁止再弹原生批准卡；被拦则换路，仍拦则记 §F 后测下一项。验证码若开着，T1.1 记阻塞并停。

浏览器工具顺序：`browser_tabs` → `navigate` → `lock` → 交互 → 关键步 snapshot/截图 → 结束 `unlock`。  
质衡 MCP 可并行：查 `testFlowId` / 失败现场；浏览器改画布与点 Run。

### B.0.1 人可复现测试铁律（重点）

> **目的**：冒烟要证明「真人用户按产品 UI 能走通」；Agent 只是代点。若用控制台改 store、直接改库、或只调 demo API 刷绿，**对验收没有意义**，还会掩盖产品 bug。



#### 允许（人手也能做）


| 动作                                                           | 说明                                   |
| ------------------------------------------------------------ | ------------------------------------ |
| 浏览器点击 / 输入 / 选场景 / 保存 / Run                                  | 有头浏览器，用户可盯着看                         |
| 质衡 **AI 助手** 发提示 → Staging 逐项 **✓ 确认 / ✕ 取消** → 保存           | 与真人造流同一路径                            |
| AI 建议的 **删节点 / 删边 / 删场景**：点 Staging **✓ 确认删除**（唯一人工确认，无二次弹窗） | 拒绝用 ✕；勿对 Staging 边按键盘 Delete 当「确认删除」 |
| 画布展示不全时：**拖拽画布**、点 **小地图**、**适应视图** / 缩放                     | 与人手调视口同一路径；先关挡着的侧栏                   |
| demo-ui 或管理 API **加载 / reset 场景**                            | 产品提供的测试支撑，人手也会点                      |
| 调试台逐步调接口                                                     | 人手可用；可作诊断，**不能单独当作 T2「画布流」勾选依据**     |
| 素材库选文件上传、HTTP 节点填 `{{asset.*.storagePath}}`                  | 测参文件产品路径                             |
| 修 **产品代码 / SQL 种子 / 文档** 后，再按上表重测                            | 正规闭环                                 |
| 截图、Run 详情、§F 记现象                                             | 证据                                   |




#### 禁止（人手做不到或等于作弊）


| 禁止                                                         | 反例                                                |
| ---------------------------------------------------------- | ------------------------------------------------- |
| `page.evaluate` / 控制台改 **Pinia / Vuex / 内存图**              | 拒幽灵边、改 `pendingEdges`、直接 `markConfirmed`          |
| 直接 **UPDATE 库里的** `graph_json` **/ 跑批 SQL「修图」** 冒充 UI 造流成功 | 除非用户明确要求修数据脚本，且仍须 UI 重跑验收                         |
| 用 **curl/脚本只打 8081** 勾选 T2 画布相关项                           | API 绿 ≠ 画布/Staging/Run 绿                          |
| 为过断言 **悄悄改断言期望或跳过节点** 却不修引擎/不记 §F                          | 掩盖缺陷                                              |
| 点击被侧栏挡住或落在视口外仍 `force` 点、用户看不见的「幽灵操作」当成功                   | 应拖拽画布 / 点小地图 / 适应视图 / 缩放，使人也能点到                   |
| **空白画布 / 空** `nodes` **冒充造流成功**                            | 新建流默认空图；Staging 未确认保存则刷新仍空。须保存后画布可见节点再勾选          |
| 节点上只填绝对本机路径冒充「产品测参路径已通」                                    | 产品主路径是素材库上传 + `{{asset.*.storagePath}}`；绝对路径仅调试兜底 |




#### 卡住时怎么做（正规顺序）

```text
1. 截图 + 写清：UI 上人手下一步会点什么、点了发生什么
2. 判断：操作问题（没找到按钮） vs 产品缺陷（点了也不对 / 无法完成）
3. 产品缺陷 → 修代码（或记 §F 阻塞）→ 重启如需 → 从同一 UI 步骤重试
4. AI 造流出错（幽灵 start 边、非法断言运算符、不支持的 JsonPath）→
   用人可点的方式：Staging ✕ 取消坏项 / 让 AI「修复本节点或重生成」/ 属性面板改规则
   → 禁止用 store 手术让边「假装」灌进画布
5. 简单 bug 直修；复杂先写方案再改（能力缺口类）
```



#### T2 / 画布项勾选口径

- **可勾**：该用例对应测试流在质衡画布上 **Staging 已清零（若有）→ 已保存且画布非空（有节点）→ Run 终态符合期望**，证据含截图或 Run 时间。  
- **不可勾**：仅 demo API / Swagger / 临时脚本通过。可在备注写「API 曾绿，画布未验」。  
- **不可勾**：顶栏仍提示「nodes 为空」、画布无节点、或只建了空流名未完成 Staging→保存。  
- **推荐造流**：提示集对应段落 → AI 助手生成 → 用户（或 Agent 代点）确认 → **保存确认有节点** → Run；顺带测画布与 AI Staging。



#### 可复制给 Agent 的提示词

```text
你正在执行 qualitest《全面测试手册》的自动测试。必须遵守手册 §B.0.1：

1. 只用真人在产品 UI 上也能完成的操作验收（浏览器点击、AI 助手、Staging 确认/取消、保存、Run、场景加载、素材库上传）。
2. 禁止：改 Pinia/内存状态、直接改 graph_json 刷绿、用仅调 demo API 的结果勾选画布相关用例、force 点击被遮挡/视口外且用户看不见的控件当成功、**空白画布（nodes 为空）冒充造流成功**。
3. T2 画布相关项：必须「场景加载 →（AI）造流或打开流 → Staging 确认干净 → **保存后画布有节点** → Run → 断言符合期望」；仅 API 绿或空图不得勾选。
4. 画布展示不全：先关侧栏，再 **拖拽画布**、点 **小地图** 或 **适应视图**/缩放，把目标挪进可视区后再点 Staging ✓。
5. 发现产品缺陷：先记 §F，能修则改产品代码并用人可复现路径重测；不要用旁路数据手术绕过。
6. AI 生成坏图（如连到不存在的 start、notempty/非法 JsonPath）：用 Staging 取消、AI 修复提示或属性面板修改；不要 evaluate 修 store。
7. 每步截图；连续同操作失败 ≥4 次停手汇报；结果按 §B.5 汇总。
8. 不等 Cursor 审批卡；产品 Staging / 素材提案 / Run 按钮在浏览器里自己点。

从手册当前断点继续；已勾选且证据不足的项应改回未勾或注明作废重测。
```



#### 与工程修复的边界


| 类型          | 做法                                                   |
| ----------- | ---------------------------------------------------- |
| 冒烟发现的产品 bug | 修代码 → UI 重测 → §F 记关闭                                 |
| 能力缺口        | 先方案，**一次做完**；合入前该项可暂记 §F / 改写断言用人可支持的语法做临时验收（须在备注标明） |
| 靶场种子 / 缺列   | 可改 SQL 种子或 patch（人手部署也会做）→ reset 场景 → 再 UI 测         |



### B.1 T1 · 最小冒烟（门禁）



#### T1.1 登录与项目

1. `browser_navigate` → `http://localhost:5173/login`
2. `admin` / `admin123`（验证码须关；开着则 T1.1 停）
3. 打开已有 demo 项目，或新建「全面测试」项目
4. 能进项目详情 / 接口 / 测试流

- [x] 登录成功并进入指定项目



#### T1.2 环境

1. 环境管理：`baseUrl = http://localhost:8081`（Compose 内质衡打宿主机 demo 用 `host.docker.internal:8081`）
2. （T3）可选开启「允许还原被测数据」→ 若开则串行

- [x] baseUrl 正确可选
- [ ] （可选）允许还原已开，且知悉串行



#### T1.3 接口调试台

1. 选 `POST /api/account/auth/login`（或等价）
2. demo 环境；`13800000001` / `Test@123456` 发送成功
3. 错误密码或失败路径 → 失败可见、非白屏

- [x] 成功与失败各至少 1 次可读



#### T1.4 测参文件上传（本地盘 · 门禁）

> **产品主路径**：素材库选文件 → 服务端本地盘（`/common/upload`）→ 条目存 `{ type, fileName, storagePath }` → HTTP form-data `type=file` 引用 `{{asset.<key>.storagePath}}` → 跑流读盘组 **真 multipart 文件 part**。  
> **不依赖 RustFS**。样例文件建议 < 1.5MB（引擎单文件约 2MB 上限）。  
> demo 的 `POST /api/file/upload` 是被测接口；质衡测参不要塞进 demo 业务 bucket。

**准备**

1. 项目内已有（或插件导入）`POST /api/file/upload` 接口资产；Body 为 form-data，至少含：
  - `bizType`（string，如 `product-image`）
  - `file`（type=`file`）
2. 本地任选一张小图（如 png）备用。

**步骤**

1. **素材库**
  - 打开项目「素材库」→ 新建素材  
  - key 例如 `smokeCover`；类型选 **file** → 「选择文件」上传刚才的小图  
  - 保存后刷新/重开：value 为 `/profile/upload/...` 路径；落库形状含 `storagePath`
2. **（可选）调试台当次发送**
  - 打开该上传接口调试：Body = form-data；`file` 选本地文件直接发送  
  - 期望：demo 返回成功（含 url/key 一类字段）；**此步只证调试发送，不代替素材落盘**
3. **测试流 HTTP 节点**
  - 新建或打开流（建议名含 `smoke-file-upload`）  
  - 绑定 `POST /api/file/upload`  
  - Body 模式 **form-data**：`bizType=product-image`；`file` 类型 **file**，value = `{{asset.smokeCover.storagePath}}`  
  - 保存节点测值（相对资产有差分则进 `requestValueOverrides`）
4. **Run**
  - 保存流 → Run → 有终态  
  - Run 详情：该步请求为 multipart；被测方收到真文件（非把路径当普通字符串字段发出）  
  - 断言可选：HTTP 成功 / 业务码成功 / 响应含 url 或 objectKey

- [x] 素材库 file：选文件上传成功，保存后 `storagePath` 可读
- [x] HTTP 节点 form-data：`type=file` + `{{asset.*.storagePath}}` 可保存
- [x] Run：读盘成功并组 multipart；终态通过（非「找不到上传文件」）



#### T1.5 靶场场景

优先 demo-ui：本地 `http://localhost:5174`（或 Compose `http://localhost:8082`）→ 登录 → 测试场景 → 加载 **S01**。  
无 demo-ui：`POST /web/test/scenario/load/S01`。

- [x] S01 加载成功
- [x] 用推荐账号打通至少 1 次客户端调用



#### T1.6 测试流一条主路径

1. 新建或打开流（建议名含 `smoke-S01`）
2. 至少 **HTTP → 断言**（可 AI 粘贴提示集 §10.1，或手工拖）
3. 保存 → Run → 有终态（非卡住）
4. Run 详情：时间线 / HTTP 面板可读

```text
客户端 13800000001 登录 → 查看购物车 cartId 5001/5002 → 结算预览 → 下单 → 余额支付 → 查询订单与个人余额，核对金额与状态一致。
请在关键步骤后添加断言：接口成功码、订单状态、支付状态、余额变动等与上文一致；提取 orderId、token 供后续节点使用。
```

- [x] 画布可见；Run 有终态；详情可读



#### T1.7 门禁


| 项         | 要求                                         |
| --------- | ------------------------------------------ |
| T1.1～T1.6 | 全绿（或有文档规避）才算门禁通过                           |
| 缺接口资产     | **阻塞**，先插件上传再续                             |
| T1.4 测参文件 | **阻塞**：素材上传 + 跑流 multipart 必须通（不要求 RustFS） |


---



### B.2 T2 · Demo 业务全量

**约定**（服从 §B.0.1）：

1. 先 **加载场景**（reset 按提示集）→ 再测。
2. **验收主路径 = 质衡画布测试流**（AI 助手造流或打开已有流 → Staging 确认 → 保存 → Run）。
3. 调试台 / Swagger / 临时脚本：仅诊断或补业务理解；**不能**单独作为本表勾选依据。
4. 成功路径验状态与金额；失败路径验业务拒绝（非白屏）；写操作串行。
5. **测试流说明**须写清楚本次在测什么（可对齐下方条目），便于列表扫读与失败对照。
  勾选格式同 T1：`- [x]` 通过 · `- [ ]` 未测 · 跳过在文末注明。



#### T2.1 认证与账号

- [x] **A1** · 基线 · 客户端登录 + 查资料 · `13800000001` 成功，手机号一致
- [x] **A2** · 基线 · 注册 `13800000099` · 成功；重复跑前需 reset
- [x] **A3** · 基线 · 管理端登录 · `admin` 供发货/审核
- [x] **A4** · **F01** · 停用账号登录 · `13800000002` 失败



#### T2.2 商品浏览

- [x] **P1** · 基线 · 分类 / 列表 / 详情 2001 · 可购 SKU、价、库存可读
- [ ] **P2** · 基线 · 详情媒体字段 · cover/detail/video · **跳过**：基线种子详情无 cover/detailImages/video 字段（见 T3.1）



#### T2.3 购物车

- [x] **C1** · **S01** · 车两行 + 预览 · cartId 5001/5002；preview **195**
- [x] **C2** · **S01** · 改数量 / 删 / 清空 · 列表与小计符合 · `smoke-C2` · `20:04:36 · 通过`
- [x] **C3** · **F02** · 下架 SPU 2002 加购 · 失败，无脏数据 · `smoke-C3`
- [x] **C4** · **F03** · 停用 SKU 2111 加购 · 失败 · `smoke-C4`



#### T2.4 订单与支付

- [x] **O1** · **S01** · 购物车结算下单 · 待付款；金额/运费一致 · `smoke-O1b` · `20:16:56 · 通过`
- [x] **O2** · **S07** · 直接购买 / 查 7006 · payAmount **57** 或等价 · `smoke-O2` · `20:18:24 · 通过`
- [x] **O3** · **S02** · 占券待付 7001 · payAmount **299**；券 6001 占券未核销 · `smoke-O3` · `20:21:57 · 通过`
- [x] **O4** · **F06** · 未满门槛用券 · 预览/下单失败 · `smoke-O4` · `20:22:18 · 通过`
- [x] **O5** · **S02**/**S07** · 余额支付 payType=3 · 已支付；余额减少 · `smoke-O5` · `20:25:13 · 通过`
- [x] **O6** · **F07** · 不足支付 7008 · 失败；仍待付 · `smoke-O6` · `20:26:59 · 通过`
- [x] **O7** · **F07** · 充值后再付 · 充值 200 成功；流水含充值+支付 · `smoke-O7` · `20:28:37 · 通过`
- [x] **O8** · **S03** · 已支付再付 · 拒绝 · `smoke-O8` · `20:30:28 · 通过`
- [x] **O9** · **S07** · 取消 7006 · 已取消；库存释放 · `smoke-O9` · `20:32:15 · 通过`
- [x] **O10** · **F04** · SKU 2101 买 5 · 整单失败 · `smoke-O10` · `20:34:11 · 通过`
- [x] **O11** · **F09** · `13800000003` 看 7009 · 拒绝 · `smoke-O11b` · `20:40:08 · 通过`
- [x] **O12** · **S04** · 确认收货 7003 · 已完成 · `smoke-O12b` · `20:45:39 · 通过`



#### T2.5 余额流水

- [x] **B1** · 基线或支付后 · 分页流水 · 倒序；可按类型筛 · 复用 `smoke-B2`/`smoke-O7` 流水查询 · `20:49:09`/`20:49:26`
- [x] **B2** · 基线/**F07** · 模拟充值 · 余额与流水一致 · `smoke-B2` · `20:49:09 · 通过`
- [x] **B3** · **F07** 或退款后 · 支付/退款流水 · 收支相符 · 复用 `smoke-O7` · `20:49:26 · 通过`



#### T2.6 优惠券

- [x] **V1** · **S08** · `13800000002` 领 3002 · 我的券可见 · `smoke-V1` · `20:55:04 · 通过`
- [x] **V2** · **F12** · 再领 3001 · 失败 · `smoke-V2` · `20:56:17 · 通过`
- [x] **V3** · 基线 · 券 6001 + 按金额查可用 · 门槛正确 · 复用 `smoke-O3`（6001 占券/我的券）· `20:21:57`
- [x] **V4** · **F05** · 过期券 · available 不含 · `smoke-V4` · `20:59:12 · 通过`



#### T2.7 退款

- [x] **R1** · **S03** · 对 7002 预览+申请 · 退款单生成 · `smoke-R1c`（apply 字面量 items 8003/8004）· `21:18:36 · 通过`
- [x] **R2** · **F11**/**S04** · 已发货退款 · canRefund=false 或拒绝 · `smoke-R2` · `21:09:51 · 通过`
- [x] **R3** · **F08** · 7005 再申请 · 失败 · `smoke-R3` · `21:11:40 · 通过`
- [x] **R4** · **S06** · 撤销 9002 · 可再申请 · `smoke-R4` · `21:12:57 · 通过`
- [x] **R5** · **S05** · 部分退后对 7004 再退 · 成功 · `smoke-R5`（items 8006×1）· `21:22:02 · 通过`
- [x] **R6** · **S06** · 管理端审核+完成 · 余额与订单态正确 · `smoke-R6c`（提取 `$.token`）· `21:21:44 · 通过`



#### T2.8～T2.10 地址 / 管理端 / 复合

- [x] **D1** · 基线 · 地址 CRUD + 默认 · 以 4001 为起点 · `smoke-D1` · `21:22:20 · 通过`
- [x] **M1** · **S03** · 发货 7002 · 客户端已发货 · `smoke-M1` · `21:26:14 · 通过`
- [x] **M2** · **S07** · 关闭待付 7006 · 成功 · `smoke-M2` · `21:26:32 · 通过`
- [x] **M3** · **F10** · 关已支付 7010 · 拒绝 · `smoke-M3`（预期业务拒绝）· `21:26:50 · 通过`
- [x] **M4** · 任意 · 订单/退款列表筛选 · 分页筛选生效 · `smoke-M4b` · **S06** · `21:49:10 · 通过`（依赖 #23 跳过空 query）
- [x] **E1** · **S01** · §10.1 购物流 · 登录→车→下单→余额付 · 复用 `smoke-S01` · `21:37:56 · 通过`
- [x] **E2** · **S03**→管理端 · §10.2 下单到退款 · 申请→审核→入账 · `smoke-R1c`（S03）+ `smoke-R6c`（S06，提示集允许切场景）
- [x] **E3** · **S03**→**S04**/**F11** · §10.3 发货后不可退 · 再退失败 · 复用 `smoke-R2` · **F11** · `21:38:14 · 通过`
- [x] **E4** · **F07** · §10.4 不足→充值→再付 · 步骤干净 · 复用 `smoke-O7` · `21:38:33 · 通过`

**T2 完成**：能加载的场景均为通过或「预期失败已验证」；缺字段/权限可跳过并写原因。**本轮 T2 除 P2 跳过外全绿。**

---



### B.3 T3 · 平台进阶

勾选格式同 T1 / T2：`- [x]` 通过 · `- [ ]` 未测 · 跳过在文末注明。

#### T3.1 商品媒体与上传（demo 业务媒体）

前置：媒体能力 + RustFS（或 demo 本地对象存储配置）；样例 `qualitest-demo/docs/fixtures/`。无则整组跳过。  
质衡侧测参文件（T1.4）走本地盘，不依赖本组。本组测的是 **demo 被测上传接口**。

- [ ] **U1** · `POST /api/file/upload`（`bizType=product-image`） · 返回 url/key
- [ ] **U2** · `bizType=product-video`（sample-product.mp4） · 成功
- [ ] **U3** · 提示集 M.1：创建有图商品 → 详情断言 · 字段一致
- [ ] **U4** · M.2 AI Diff 升级旧无图流 · 合并变画布；取消不脏写



#### T3.2 数据库快照 / 还原（test-support + 质衡 Run）

> 验的是：**被测库**在写节点前打 checkpoint，失败后可还原再重试。  
> 质衡不直连被测库，只调 demo 的 `POST /test-support/snapshot`、`/restore`。  
> **开启「允许还原被测数据」后，同一环境必须串行跑**，勿并行多条带快照的 Run。

**前置（缺一则整组跳过，并在备注写原因）**

1. demo `application-dev.yml`：`qualitest.test-support.enabled=true`（且非 prod profile）
2. demo 已实现快照策略（开发默认 `MysqldumpSnapshotStrategy`：本机 `mysqldump` / `mysql` 可用，快照目录可写）
3. 质衡环境管理：该环境 **开启「允许还原被测数据」**（`allowDestructiveReset=1`）；`baseUrl` 指向 `http://localhost:8081`
4. 准备一条含 **写操作** 的流（如下单改库存），或临时改断言让后续节点必失败

---



##### A. 端点直测（不经画布，证被测方能力）

```text
# 1. 打快照
curl -s -X POST http://localhost:8081/test-support/snapshot \
  -H "Content-Type: application/json" \
  -d "{\"scope\":\"tables\",\"tables\":[\"mall_order\",\"mall_product_sku\"],\"label\":\"manual-t3:node-1\"}"
# 记下响应 snapshotId（如 snap-…）

# 2. 用客户端或 Swagger 做一次写操作，改脏被测数据（如改购物车数量 / 下单）

# 3. 还原
curl -s -X POST http://localhost:8081/test-support/restore \
  -H "Content-Type: application/json" \
  -d "{\"snapshotId\":\"<上一步 snapshotId>\"}"
# 期望：{"status":"restored", ...}

# 4. 再查业务数据：应回到快照时点；同一 snapshotId 再 restore 一次仍成功（幂等）
```

- [ ] **DB1** · `POST /test-support/snapshot` · 返回 `snapshotId`，status 就绪  
- [ ] **DB2** · 写脏数据后 `POST /test-support/restore` · `status=restored`，业务数据回滚  
- [ ] **DB3** · 同一 `snapshotId` 再 restore · 幂等成功  

---



##### B. 质衡产品路径（画布 Run · 人可复现）

```text
1. 环境：允许还原被测数据 = 开；baseUrl = http://localhost:8081
2. 打开/新建测试流（建议名含 smoke-snapshot）
3. 选一个会写库的 HTTP 节点（如下单）→ 属性勾选「执行前快照」(snapshotBefore)
4. 其后故意接一个必失败节点（错误断言 / 错误路径），或临时改断言保证失败
5. 保存 → Run
6. 期望：
   - Run 详情出现 snapshot 审计步（打到 checkpoint）
   - 写节点可能成功，后续失败 → Run 进入 paused（暂停等待决策）
7. 在 Run 详情点「还原并重试」(restoreAndRetry)
8. 期望：
   - 出现 restore 审计步
   - 被测库回到该节点快照前
   - 从失败节点（或约定节点）继续跑；可再失败再决策，或改断言后重试至通过
9. （对照）环境关闭「允许还原」后重跑同流：snapshot/restore 静默跳过，不报错拖垮 Run
```

- [ ] **S1** · 环境允许还原 + 节点 `snapshotBefore` · 流可保存  
- [ ] **S2** · Run：有 snapshot 步；后续失败 → `paused`  
- [ ] **S3** · 选「还原并重试」· 有 restore 步；数据已回滚；Run 能继续或结束  
- [ ] **S4** · （可选）决策其它按钮：原地重试 / 跳过 / 中止 · 行为符合预期  
- [ ] **S5** · （可选）关闭允许还原 · checkpoint/restore 静默跳过，不误伤主路径  

**不过则**：文档/README 弱化「还原」宣传；记 §F，不挡 T1。

#### T3.3 AI Diff（需 LLM）

无 Key → 跳过。

- [ ] **I1** · 自然语言改一处 · Diff / Staging
- [ ] **I2** · 确认合并 · 画布已落库
- [ ] **I3** · 取消 / Reject · 不脏写



#### T3.4 qualitest MCP（只读）

配置见 [mcp.md](./mcp.md)。

- [ ] **Q1** · `list_flows` · 有 testFlowId
- [ ] **Q2** · `get_graph_summary` · 节点/主路径摘要
- [ ] **Q3** · `get_run_failure`（若有） · 失败步骤与建议
- [ ] **Q4** · `search_apis` / `list_project_envs` / `list_asset_variables` · 非空或合理空



#### T3.5 Compose 靶场（可选）

见 [compose-测试指南](../../qualitest-demo/docs/compose-测试指南.md)。

- [ ] **K1** · `scripts\quick-start.bat` → 8082 可登录、8081 Swagger 可开
- [ ] **K2** · （可选）rustfs：9001 可登录；上传可用
- [ ] **K3** · `docker compose down` 停干净



#### T3.6 单元 / 前端（工程向，不挡冒烟）

```text
mvn test -DskipTests=false -pl qualitest-system -am -Dtest=FlowHttpRequestBuilderFormDataTest,TestProjectApiEffectiveConfigResolverTest,HttpNodeRequestValueOverridesSupportTest
mvn test -DskipTests=false -pl qualitest-system -am -Dtest=*Flow*Test
cd qualitest-ui && yarn test
```

- [ ] **UT1** · form-data / profile 路径 / 测值叠层相关单测通过
- [ ] **UT2** · `*Flow*Test` 相关通过
- [ ] **UT3** · `qualitest-ui` · `yarn test` 通过

---



### B.4 Agent 推荐顺序

```text
自检端口 + 重申 §B.0.1
  → T1.1～T1.3
  → T1.4 测参文件（素材上传 → 节点引用 → Run multipart）
  → T1.5～T1.6（门禁不过则停或只修阻塞；画布项须 Run 过）
  → T2：A→P→C→O→B→V→R→D/M/E 逐条：
        加载场景 → AI/已有流 → Staging 确认 → 保存 → Run → 勾表
        （禁止仅 API 勾选；卡住则修产品或 §F，禁止 store 旁路）
  → T3.1 媒体 → T3.2 数据库快照/还原（先 DB1～DB3 端点，再 S1～S5 画布）
        → T3.3 AI → T3.4 MCP → T3.5 Compose → T3.6 单测
  → 输出 B.5 汇总 + §F
```



### B.5 结果汇总（Agent 每轮结尾贴出）


| 档   | 结果  | 说明 |
| --- | --- | -- |
| T1  | **通过** | T1.1～T1.6 全绿；允许还原未开（非阻塞） |
| T2  | **通过** | A～E 全绿；**P2 跳过**（基线无媒体字段，见 T3.1） |
| T3  | 未跑  | 本轮不跑 |



| 用例 ID | 场景 | 结果 | 证据（截图/RunId） | 备注 |
| ----- | -- | -- | ------------ | -- |
| T1.1 | 商城 Demo | 通过 | `2088803096141680640` | — |
| T1.2 | 默认环境 | 通过 | `baseUrl=http://localhost:8081` | — |
| T1.3 | 登录调试 | 通过 | 成功 token；失败「手机号或密码错误」 | — |
| T1.4 | smoke-file-upload | 通过 | multipart size=70；断言通过 | — |
| T1.5 | S01 | 通过 | demo-ui 5174；cart 5001/5002 | — |
| T1.6 | smoke-S01 | 通过 | **`19:28:23 · 通过`** | flowSeed productId 须 2001 |
| A1～A4 | 见上 | 通过 | 画布 Run | A4 预期业务拒绝模板 |
| P1 | 基线 | 通过 | — | P2 跳过 |
| C1～C4 | S01/F02/F03 | 通过 | C2 `20:04:36` | — |
| O1～O12 | 各场景 | 通过 | 见 T2.4 勾选 | — |
| B1～B3 | — | 通过 | 复用 O7/B2 | — |
| V1～V4 | S08/F12/F05 | 通过 | V3 复用 O3 | — |
| R1 | S03 | 通过 | `smoke-R1c` · `21:18:36` | apply 必填 items |
| R2 | F11 | 通过 | `smoke-R2` · `21:09:51` | — |
| R3 | F08 | 通过 | `smoke-R3` · `21:11:40` | — |
| R4 | S06 | 通过 | `smoke-R4` · `21:12:57` | — |
| R5 | S05 | 通过 | `smoke-R5` · `21:22:02` | — |
| R6 | S06 | 通过 | `smoke-R6c` · `21:21:44` | 管理端提取 `$.token` |
| D1 | 基线 | 通过 | `smoke-D1` · `21:22:20` | — |
| M1～M3 | S03/S07/F10 | 通过 | `21:26:14`～`21:26:50` | M3 预期拒绝 |
| M4 | S06 | 通过 | `smoke-M4b` · `21:49:10` | #23 空 query 修复后 |
| E1 | S01 | 通过 | 复用 `smoke-S01` · `21:37:56` | — |
| E2 | S03+S06 | 通过 | R1c + R6c | 提示集允许切场景 |
| E3 | F11 | 通过 | 复用 R2 · `21:38:14` | — |
| E4 | F07 | 通过 | 复用 O7 · `21:38:33` | — |



| 结论       | 含义             |
| -------- | -------------- |
| T1 全绿    | 门禁通过，可继续 T2/T3 |
| T2 部分失败  | 记 §F；按优先级修     |
| T3 跳过/失败 | 按能力标注可选        |


---



## F. 问题记录（现场填）


| #   | 步骤 / 用例 ID                   | 现象                                                                                                                       | 是否阻塞门禁         | 处理                                                                                                            |
| --- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------------- |
| 1   | E1 / 断言过滤器                   | 引擎仅点分路径；`notempty` 静默失败                                                                                                  | 否              | **已关闭**：完整 JsonPath 已落地                                                                                       |
| 2   | T1.6 / AI 首轮失败无回复            | SSE `type=error` 被吞成「未收到设计结果」；catch 只写 alert、不落助手气泡；空 summary 时只有思考无正文                                                   | 否              | **已关闭**：`consumeSseStream` 上抛业务错误；失败/空 summary 写入助手气泡                                                         |
| 3   | T1.6 / 断言 eq                 | `payAmount` 左值 `195.0`(Double) 与右值 `195`(Long) 不相等                                                                       | 是              | **已关闭**：`CompareRuleEvaluator.compareEquals` 对 Number 按数值比较                                                   |
| 4   | T1.6 / 断言 lt 右值              | 右值裸写 `flow.balanceBefore` 被当字面量，`toDouble`→0，lt 误失败                                                                      | 是              | **已关闭**：右值支持裸作用域路径解析                                                                                          |
| 5   | T1.6 / 断言运算符别名               | AI 产出 `lessthan`/`lessThan` 未归一到 `lt`，比较恒失败                                                                              | 是              | **已关闭**：`normalizeOperator` 增加 lt/gt/lte/gte 英文别名                                                             |
| 6   | T1.3 / 数据结构参数值               | 「数据结构」填参后发送仍用空「请求示例」；发送只读 `bodyJsonText`；首轮修复把 flush 塞进 JSON catch 误报「格式无效」；树改参被示例旧非空值盖住                                 | 是              | **已关闭**：发送读树 `buildDebugExampleFromTree` + preferFiller；非法 JSON 且无 schema 值才报格式无效                             |
| 7   | T1.3 / form-data             | Body 选 form-data 仍发出 `application/json`（axios 1.x 默认 POST Content-Type 把 FormData JSON 序列化）                              | 否              | **已关闭**：`debugTransport` 对 FormData 设 `Content-Type: false` 并清默认头                                             |
| 8   | T1.6 / AI 最大步数               | 从零搭 §10.1 购物流时 Agent 仅 8 轮 tool 即中止，未出 Staging                                                                           | 是              | **已关闭**：`DEFAULT_MAX_AGENT_STEPS` 8→20                                                                        |
| 9   | T1.6 / 侧栏与 AI 布局             | 全局 `aside{padding:8px 24px;line-height:32px}` 污染画布 `<aside>`：右栏折叠后仍约 48px，「运行配置」竖排挤压；AI 助手内提示词区文字被撑乱                     | 是              | **已关闭**：移除全局 aside 演示样式；flow 侧栏 / AiChatShell 重置 padding 并强化 is-closed                                        |
| 10  | T1.6 / Staging 确认「节点平移」      | 单单元 confirm 整表替换 graph_json，未确认节点在 inFlight 期间短暂消失；再叠加 `focusNodeIds` 视口动画，看起来像节点在跑                                      | 是              | **已关闭**：`applyConfirmedGraph` 合并保留 pending Staging；`focusNodeIds` 默认仅离屏才平移；连点 `confirmInFlight` 竞态加同步锁        |
| 11  | T1.6 / 改图却无 Staging          | 用户明确要求改图/修复时，模型只写自然语言方案未调 `submit_flow_design_patch`（`explainOnly`）；UI 无提示，易误以为已改图                                       | 否              | **已关闭（体验）**：助手气泡标明「本轮未提交 Staging」；system prompt 禁止「写方案不 submit」                                               |
| 12  | T2 / Staging 删除确认闪烁          | `deleteEdge` 等在 Staging ✓ 后再弹 `ElMessageBox`；连点多 ✓ + 点「取消」导致多弹窗叠层闪烁，似卡死                                                  | 否              | **已关闭**：去掉删除二次 MessageBox（Staging ✓=唯一确认）                                                                     |
| 13  | T2.3 C1 / 结算预览               | AI 把 `cartIds`/`addressId` 写在 `requestValueOverrides` 顶层（非 `bodyExample`），运行时静默忽略；叠加资产默认体同时含 `items`+`cartIds` → `不可同时传` | 否              | **已关闭**：`normalizeOverridesShape` 落盘/叠层前提升误放字段；默认 body 修正                                                     |
| 14  | T2.3 C2 / Staging 断言         | 设计期用响应 **example** 试算硬拦；schema 占位 `cartId:0` 导致过滤器假失败                                                                    | 否              | **已关闭**：门禁改按 **响应 schema** 校验                                                                                 |
| 15  | T2.3 C4 / Staging pending 叠层 | 多轮 AI 造流时 pending 相对空底图同位放置，✓ 被挡难点                                                                                       | 否              | **已关闭**：hydrate 前 AABB 避让 + 服务端 normalize 重叠也错开                                                               |
| 16  | T1.4 / 测参文件                  | Body 面板 `body` getter 每次 `ensureBodyShape` 深拷贝 `formData`，`DebugKvSheet` 就地改 `row.value` 写到临时副本，UI 输入立即被刷空，测值无法落盘        | 是              | **已关闭**：`HttpConfigBodyPanel.vue` getter 返回 `props.modelValue` 同一引用；emit 时再 `ensureBodyShape`（未提交合入前以工作区改动为准） |
| 17  | T1.6 / explainOnly 无 Staging | 同会话多轮从零搭 §10.1 时，模型反复拉接口详情（订单 schema 过大）后只写方案不 `submit_flow_design_patch`；偶发摘要「AI 已生成测试流修改建议」但画布无 Staging    | 是（直至有 Staging） | **已缓解**：`get_api_details` 批量；造流 Agent 剩余步数 ≤2 催 submit；达上限文案提示 submit；UI「本轮未提交 Staging」气泡。冒烟仍建议新建对话 + §10.1 短提示 + API id |
| 18  | T1.6 / AI patch 体与金额         | 首轮 patch 结算预览/下单 body 同时含 `cartIds`+`items` → 业务码「须二选一」；断言误写 `payAmount=1954`（提示换行粘连）                                    | 否              | **已关闭（本轮）**：AI「Run 失败修复」改 body 仅 `cartIds`+`addressId`、金额改 195 → Run 通过；资产默认体双字段问题亦见 #13                      |
| 19  | T1.6 / 余额流水断言符号              | AI 写 `changeAmount==-195`；demo 流水 `changeAmount` 为正数、方向由 `changeType=2`（支出）表达 → 过滤器 `[]`                                 | 是（直至改断言）       | **已关闭（本轮）**：AI 修复改为 `changeAmount==195` + `recordType==3 && changeType==2`；重载 S01 后 `22:50:07 · 通过`           |
| 20  | T1.4 / 跑流 Bearer 占位符         | `EffectiveApiConfig.toApiView()` 未拷贝 `authConfig` → 跑流时 `null` 被当成 inherit，免登录登录接口仍补 `Bearer {{flow.token}}` → `TF_PLACEHOLDER_UNDEFINED` | 是              | **已关闭**：`toApiView` 增加 `view.setAuthConfig(source.getAuthConfig())`；单测覆盖；重跑 `11:48:31 · passed`                   |
| 21  | T2.1 A3 / 管理端 `/login` 鉴权   | 资产 `auth_config` 误为 inherit Bearer；Staging 给登录节点补 `Authorization`，且与 getInfo 的 `flow.token` 来源校验打架；RuoYi token 在 `$.token` 非 `$.data.token` | 否              | **已关闭（产品）**：`test_project_template` + 预制 `apis[].authConfig.mode=none` / `loginHint`；有配置后不再全局猜 `/login`（空配置暂留 builtin）。前端模板库/新建勾选见未修复项 §8 |
| 22  | T2.1 A4 / body 与业务码         | AI 登录体写成 `{{asset.clientAuth.*}}`（主测号）→ 停用账号场景仍 200；失败路径须关「业务 Code 校验」再断言 `code≠200`，否则 Run 在业务码 500 处直接失败 | 否              | **已关闭（产品·模板）**：快捷模板「预期业务拒绝」+ system prompt；场景测值 vs 素材库不做硬限。现场曾手改 → `12:52:30 · 通过` |
| 23  | T2.8 M4 / 空 query `params` | OpenAPI 把 RuoYi `BaseEntity.params`(Map) 同步成 query；跑流发出 `params=` 空串 → 被测端 500「String→Map」 | 否（挡 M4） | **已关闭**：`FlowHttpRequestBuilder.buildQueryString` 跳过 blank 值；单测 `build_skipsBlankQueryParams`；`smoke-M4b` · `21:49:10 · 通过`（JRebel 热更后复测） |
| 24  | T2.7 R6 / 管理端提取路径 | AI 常把管理端登录提取写成 `$.data.token`（客户端习惯）→ `{{flow.token}}` 未定义；提取名须为 `token` 不能 `adminToken`（Staging 登录口校验） | 否 | **已关闭**：loginHint/可用 schema 才写路径，不再单段补 `$.data`；双端登录口认 `adminToken`+`$.token`；Map schema 不编路径、跑流后 AI 修。单流双端托管头见未修复项 §7 |


---



## 附录 · 相关资源


| 资源                       | 路径                                                                                               |
| ------------------------ | ------------------------------------------------------------------------------------------------ |
| AI 提示正文                  | [qualitest-demo/docs/ai-test-flow-prompts.md](../../qualitest-demo/docs/ai-test-flow-prompts.md) |
| 场景 manifest              | `qualitest-demo/sql/seed/scenarios/manifest.json`                                                |
| MCP                      | [mcp.md](./mcp.md)                                                                               |
| 测试流节点                    | [test-flow-nodes.md](./test-flow-nodes.md)                                                       |
| 快照 Starter               | [test-support-starter/README.md](../../qualitest-demo/test-support-starter/README.md)            |
| Demo Compose 测           | [compose-测试指南.md](../../qualitest-demo/docs/compose-测试指南.md)                                     |
| 样例附件                     | `qualitest-demo/docs/fixtures/`                                                                  |


