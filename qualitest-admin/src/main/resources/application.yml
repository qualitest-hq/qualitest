# 入口配置：仅指定默认环境；dev / prod / docker 的完整项在对应 profile 文件中
spring:
  profiles:
    # 本地默认 dev；生产 -Dspring.profiles.active=prod；容器 -Dspring.profiles.active=docker
    active: ${SPRING_PROFILES_ACTIVE:dev}
