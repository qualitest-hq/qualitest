---
import { type Lang, useTranslations } from "../i18n/ui";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<section class="section" id="ecosystem">
  <div class="wrap">
    <h2 class="section-title">{t.ecoTitle}</h2>
    <p class="section-lead">{t.ecoLead}</p>

    <ul class="repos">
      {
        t.ecoRepos.map((repo) => (
          <li>
            <div class="row">
              <span class="meta">
                <span class="name">{repo.name}</span>
                <span class="role">{repo.role}</span>
              </span>
              <span class="links">
                <a href={repo.href}>{t.ecoGitHub}</a>
                <a href={repo.giteeHref}>{t.ecoGitee}</a>
              </span>
            </div>
          </li>
        ))
      }
    </ul>
  </div>
</section>

<style>
  .repos {
    list-style: none;
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--line);
  }

  .row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.15rem 0.35rem;
    border-bottom: 1px solid var(--line);
    transition: background 0.2s ease, padding-left 0.25s var(--ease);
  }

  .row:hover {
    background: rgba(255, 255, 255, 0.45);
    padding-left: 0.55rem;
  }

  .row:hover .name {
    color: var(--accent-deep);
  }

  .meta {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    min-width: 0;
  }

  @media (min-width: 720px) {
    .meta {
      flex-direction: row;
      align-items: baseline;
      gap: 1.25rem;
    }
  }

  .name {
    font-family: var(--font-display);
    font-weight: 650;
    font-size: 1.08rem;
    letter-spacing: -0.01em;
    transition: color 0.2s ease;
  }

  .role {
    color: var(--muted);
    font-size: 0.94rem;
  }

  .links {
    display: flex;
    flex-shrink: 0;
    gap: 0.85rem;
  }

  .links a {
    color: var(--muted);
    font-size: 0.92rem;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .links a:hover {
    color: var(--accent);
  }
</style>
